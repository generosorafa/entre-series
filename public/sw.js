const CACHE='entre-series-v5';
const FILES=['./','./index.html','./styles.css','./app.js','./timer.js','./audio.js','./push.js','./config.js','./manifest.webmanifest','./privacy.html','./icon.svg','./icons/icon-192.png','./icons/icon-512.png','./icons/apple-touch-icon.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(FILES)))});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('entre-series-')&&key!==CACHE)await caches.delete(key);await self.clients.claim()})())});
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(event.request.method!=='GET'||url.origin!==self.location.origin||!url.href.startsWith(self.registration.scope))return;event.respondWith((async()=>{const cached=await caches.match(event.request);if(cached)return cached;return fetch(event.request)})())});
self.addEventListener('push',event=>{event.waitUntil((async()=>{let data={};try{data=event.data?.json()||{}}catch{}const expires=Number(data.expires),late=expires&&Date.now()>expires;await self.registration.showNotification('ENTRE SÉRIES',{body:late?'Seu descanso anterior terminou. Este aviso chegou com atraso.':'Descanso concluído. Hora da próxima série.',icon:'./icons/icon-192.png',badge:'./icons/icon-192.png',tag:'xamai-rest',data:{url:self.registration.scope},renotify:true})})())});
self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil((async()=>{const list=await self.clients.matchAll({type:'window',includeUncontrolled:true});const tab=list.find(client=>client.url.startsWith(self.registration.scope));if(tab)return tab.focus();return self.clients.openWindow(self.registration.scope)})())});
